% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vsp_ndp_functions.R
\name{define_vsp_param_space}
\alias{define_vsp_param_space}
\title{Define Variable-Specific Parameter Spaces for Virtual Species Simulations}
\usage{
define_vsp_param_space(
  param_ranges,
  raster_stack = NULL,
  raster_dim = c(30, 30),
  save_to = NULL
)
}
\arguments{
\item{param_ranges}{A named list where each element is a list of vectors: \code{a}, \code{b}, \code{alpha}, \code{gamma} for a variable.}

\item{raster_stack}{Optional \code{SpatRaster} with layers named as in \code{param_ranges}. If NULL, random rasters are created.}

\item{raster_dim}{Dimensions (rows, cols) of random rasters if \code{raster_stack} is not provided (default = c(30, 30)).}

\item{save_to}{Optional folder path to save each variable's parameter grid as a .csv file.}
}
\value{
A list with:
\describe{
\item{env_raster}{A \code{SpatRaster} stack of environmental layers}
\item{parameter_grids}{A named list of parameter \code{tibble}s per variable}
}
}
\description{
Builds a list of parameter grids (a, b, alpha, gamma) for each environmental variable used in beta response simulations. Optionally generates a matching raster stack with values sampled within each variable's domain.
}
